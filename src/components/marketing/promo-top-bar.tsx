import { WealthPromoBanner } from "@/components/marketing/wealth-promo-banner";

/** Top-of-site promotional strip — shown on every marketing page via header */
export function PromoTopBar() {
  return <WealthPromoBanner variant="strip" />;
}
